import 'package:appgames/pantallas/main_psp_pantalla.dart';

import 'package:flutter/material.dart';

class MainPantalla extends StatelessWidget {
  const MainPantalla({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: MainPspPantalla());
  }
}
